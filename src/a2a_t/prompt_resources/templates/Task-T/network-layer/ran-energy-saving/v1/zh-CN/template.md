## 操作类型(Operation Type)

{{操作类型}}（必填）

要求：

1. 请提供操作类型，取值范围：创建、修改

## 任务类型(Task Type)

无线网络节能

## 任务描述(Task Description)

{{任务描述}}（必填）

要求：

1. 请根据<操作类型>（创建/修改），动态提供任务描述信息：
   - 类型为【创建】时内容为：基于<任务对象>、<任务上下文>，输出无线网络节能方案，达成<任务目标>中定义的无线网络节能目标，并按照<预期输出>中定义的格式返回无线网络节能结果。
   - 类型为【修改】时内容为：基于<任务对象>、<任务上下文>，输出无线网络节能方案，达成<任务目标>中定义的无线网络节能目标。

## 任务对象(Task Object)

{{任务对象}}（必填）

要求：

1. 区域名称：（必填）
   - 明确指定节能覆盖范围信息，地理或逻辑区域（如 区级行政单位）
   - 必须是类似"松山湖管委会"这种具体且物理真实的小区名称，而不是"所有小区"、"室内小区"、"室外小区"这种无具体物理位置的描述
   - 举例：区域："松山湖管委会"

## 任务目标(Task Target)

{{任务目标}}（选填）

要求：

1. 定义任务需达成的量化或定性目标，量化目标和定性目标不可同时选择，默认为定性目标。节能目标的具体说明如下：
   - 量化目标：能耗目标；节能的增益目标，目标值格式必须为百分比，如总功耗降低的百分比；举例：能耗目标：30%
   - 定性目标：在满足任务上下文的条件下进行最大化节能

## 任务上下文(Task Context)

{{任务上下文}}（选填）

要求：

1. 节能的小区制式(LTE/NR/全部)；举例：节能的小区制式：NR
2. 需要执行节能的时间段区间范围，以天为周期循环执行，缺省则进行全时段节能。**仅在节能开启时间范围内保障速率目标**；举例：需要节能的时间范围：起始时间"08:00:00"，结束时间"12:00:00"
3. 可在节能时关闭载波的频点；举例：下行频点号：1650，1600
4. 速率保障目标：需要保障的下行吞吐率目标最小值及生效时间段。

   - **针对区域级设置方式**，需要填写以下信息：

     方式一：
     - 分时段速率保障目标(必填)，未设置速率保障目标时间段，默认采取用户体验无损方式节能，最多设置三种速率值
     - 举例：分时段速率保障目标：{00:00~06:00,2Mbps}、{06:00~18:00,10Mbps}

     方式二：
     - 全时段速率保障目标(必填)
     - 举例：速率保障目标：10Mbps

   - **针对站点级设置方式**，需要填写以下信息：
     - 站点列表(必填)
     - 分时段速率保障目标(必填)，未设置速率保障目标时间段，默认采取用户体验无损方式节能，最多设置三种速率值
     - 背景事件与事件持续时间（选填）
     - 举例：
       - 站点列表1：["站点名称1"，"站点名称2"]，分时段速率保障目标：{00:00~06:00,2Mbps}、{06:00~18:00,10Mbps}，背景事件：在00:00~18:00停电。
       - 站点列表2：["站点名称3"，"站点名称4"]，分时段速率保障目标：{01:00~06:00,2Mbps}、{06:00~18:00,10Mbps}，背景事件：在01:00~18:00停电。

## 预期输出(Expected Output)

{{预期输出}}（创建时选填，修改时无需填写）

要求：

1. 若任务对象为地理/逻辑区域时，输出网络节能任务的意图报告，意图报告应包含以下信息：

### 意图报告基本信息

1. 网络节能报告的唯一标识
2. 节能任务唯一标识
3. 总体意图的执行结果，执行结果包括执行结果状态，执行结果详情，如果未达成目标，描述未达成的具体原因

### 意图报告详细信息

1. 具体KPI的执行结果，执行结果包括执行结果状态，执行结果详情，如果未达成目标，描述未达成的具体原因
